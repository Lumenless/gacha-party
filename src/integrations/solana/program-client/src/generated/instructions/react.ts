/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { GACHA_PARTY_ROOM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const REACT_DISCRIMINATOR = new Uint8Array([30, 51, 119, 72, 84, 37, 204, 207]);

export function getReactDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(REACT_DISCRIMINATOR); }

export type ReactInstruction<TProgram extends string = typeof GACHA_PARTY_ROOM_PROGRAM_ADDRESS, TAccountRoom extends string | AccountMeta<string> = string, TAccountPlayer extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountRoom extends string ? WritableAccount<TAccountRoom> : TAccountRoom, TAccountPlayer extends string ? ReadonlySignerAccount<TAccountPlayer> & AccountSignerMeta<TAccountPlayer> : TAccountPlayer, ...TRemainingAccounts]>;

export type ReactInstructionData = { discriminator: ReadonlyUint8Array; reaction: number;  };

export type ReactInstructionDataArgs = { reaction: number;  };

export function getReactInstructionDataEncoder(): FixedSizeEncoder<ReactInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['reaction', getU8Encoder()]]), (value) => ({ ...value, discriminator: REACT_DISCRIMINATOR }));
}

export function getReactInstructionDataDecoder(): FixedSizeDecoder<ReactInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['reaction', getU8Decoder()]]);
}

export function getReactInstructionDataCodec(): FixedSizeCodec<ReactInstructionDataArgs, ReactInstructionData> {
    return combineCodec(getReactInstructionDataEncoder(), getReactInstructionDataDecoder());
}

export type ReactInput<TAccountRoom extends string = string, TAccountPlayer extends string = string> =  {
  room: Address<TAccountRoom>;
player: TransactionSigner<TAccountPlayer>;
reaction: ReactInstructionDataArgs["reaction"];
}

export function getReactInstruction<TAccountRoom extends string, TAccountPlayer extends string, TProgramAddress extends Address = typeof GACHA_PARTY_ROOM_PROGRAM_ADDRESS>(input: ReactInput<TAccountRoom, TAccountPlayer>, config?: { programAddress?: TProgramAddress } ): ReactInstruction<TProgramAddress, TAccountRoom, TAccountPlayer> {
  // Program address.
const programAddress = config?.programAddress ?? GACHA_PARTY_ROOM_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { room: { value: input.room ?? null, isWritable: true }, player: { value: input.player ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.room), getAccountMeta(accounts.player)], data: getReactInstructionDataEncoder().encode(args as ReactInstructionDataArgs), programAddress } as ReactInstruction<TProgramAddress, TAccountRoom, TAccountPlayer>);
}

export type ParsedReactInstruction<TProgram extends string = typeof GACHA_PARTY_ROOM_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
room: TAccountMetas[0];
player: TAccountMetas[1];
};
data: ReactInstructionData; };

export function parseReactInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedReactInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { room: getNextAccount(), player: getNextAccount() }, data: getReactInstructionDataDecoder().decode(instruction.data) };
}